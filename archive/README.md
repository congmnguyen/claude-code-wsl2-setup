# Archive

Pieces that used to be part of the active setup and were dropped as I moved toward a
cleaner Claude Code config (no global `CLAUDE.md`, no skills, no MCP servers unless a
project truly needs one).

- **`codex-delegate`** (agent, skill, `codex-run.sh`, doc, assets) — 2026-08-21. Stopped
  delegating implementation work from Claude to Codex; the current model is strong enough
  that the orchestration overhead wasn't paying for itself. Kept here for reference in case
  the tradeoff flips back. The companion repo `codex-wsl2-setup` this used to point to has
  been deleted.
